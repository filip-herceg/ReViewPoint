[tool:pytest]
markers =
    jwt: tests related to JWT token functionality
    performance: tests that measure performance and may take longer to run
    requires_real_db: tests that require a real database (not in-memory SQLite)
    asyncio: mark tests that use asyncio
    slow: tests that take longer than usual to run